package v8marshaler

const TagSeparator = ","
const TagNamespace = "v8"
const CommandFieldName = "command"

const (
	TagArgument = "arg"
	TagInherit  = "inherit"
	TagOptional = "optional"
	TagIgnore   = "-"
)
